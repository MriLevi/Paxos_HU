<title> Basic Paxos implementation </title>

This is a very basic paxos implementation. 

It requires python 3.10 to run because of the match-case statements.

usage:

```python main.py <filename>```

